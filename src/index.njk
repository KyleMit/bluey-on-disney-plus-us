---
title: Bluey Episodes
---
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>🐾</text></svg>">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@400;600;700&display=swap" rel="stylesheet">
  <style>{% include "style.css" %}</style>
</head>
<body>
  <div class="container">
    <h1>{{ title }}</h1>
    <p class="subtitle">Bluey Episode Numbers on Disney+ in the US</p>

    <div class="search-wrapper">
      <div class="search-container">
        <input type="text" id="search" placeholder="Search episodes..." autofocus>
        <button type="button" id="clear-search" class="clear-btn" aria-label="Clear search">✕</button>
      </div>
    </div>

    <div id="episodes-list">
      {% for episode in episodes %}
      <a href="https://www.disneyplus.com/play/{{ episode.disneyPlusId }}" class="episode" data-title="{{ episode.title }}" target="_blank" rel="noopener">
        <span class="episode-number">S{{ episode.season }} E{{ episode.episode }}</span>
        <span class="episode-title">{{ episode.title }}</span>
      </a>
      {% endfor %}
    </div>
  </div>

  <script>{% include "script.js" %}</script>
</body>
</html>
